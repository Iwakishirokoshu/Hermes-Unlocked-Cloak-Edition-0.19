# Required for WebSocket upgrade in cloak-cdp-proxy.
# IMPORTANT: do NOT reuse $http_connection — that shadows the real
# Connection request header and breaks nginx map evaluation.
map $http_upgrade $cloak_connection_upgrade {
    default upgrade;
    ''      close;
}
